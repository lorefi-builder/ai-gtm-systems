# Project Assessment Agent — backend dependencies
# Block 1 uses httpx + python-dotenv (DB client + seeding).
# Block 2 (agents) adds anthropic + pydantic.
# fastapi / uvicorn are pinned now for the later API block so the environment
# is stable across the demo.

httpx==0.27.2
python-dotenv==1.0.1
anthropic==0.39.0
pydantic==2.9.2
fastapi==0.115.5
uvicorn==0.32.1
